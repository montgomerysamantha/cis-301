∃ x ∃ y p(x, y) ⊢ ∃ y ∃ x p(x, y)
{
    1. ∃ x ∃ y p(x, y)          premise
    2. {
        3. x0 ∃ y p(x0, y)      assume
        4. {
            5. y0 p(x0, y0)     assume
            6. ∃x p(x, y0)      ∃i 5 x0
            7. ∃y ∃x p(x, y)    ∃i 6 y0
        }
        8. ∃y ∃x p(x, y)        ∃e 3 4
    }
   9. ∃y ∃x p(x, y)            ∃e 1 2
}